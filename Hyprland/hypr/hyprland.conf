#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

# autogenerated = 1 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,auto,auto
monitor=DP-3, 1920x1080, 2880x0, 1

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Enable hycov
# plugin = /usr/lib/libhycov.so

# cliphist
exec-once = wl-paste --type text --watch cliphist store #Stores only text data

exec-once = wl-paste --type image --watch cliphist store #Stores only image data

exec-once = cliphist wipe 

# Reload hyprpm to manage plugin 
# exec-once = hyprpm reload

# Execute of udiskie
exec-once = udiskie -an --no-tray -f nemo

# Execute of waybar
exec-once = waybar

# Execute of daemon for notification
exec-once = mako

# Set background
exec-once = swaybg -i ~/.config/hypr/pxfuel.jpg

# Execute program to set the temperature of the screen
exec-once = wl-gammarelay-rs

# Execute battery notifier
exec-once = bash ~/Documents/Dotfiles/Script/battery_notify.sh

# Boot with redshift
exec-once = bash ~/Documents/Dotfiles/Script/toogle_temperature.sh

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = it
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    touchpad {
        natural_scroll = yes
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

misc {
    disable_hyprland_logo = true
    vfr = true
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 10
    border_size = 2
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    layout = dwindle
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 10
    
    # blur_size = 3
    # blur_passes = 1
    # blur_new_optimizations = on

    drop_shadow = false
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = no

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = on
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device {
  name = epic-mouse-v1
  sensitivity = -0.5
}

# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Q, exec, foot
bind = $mainMod, k, killactive,
bind = $mainMod, L, exec, swaylock --clock --color 000000
bind = $mainMod, E, exec, bash ~/.config/rofi/launchers/type-5/launcher.sh
#bind = $mainMod, V, togglefloating,
bind = $mainMod SHIFT, S, exec, shutdown now
bind = $mainMod SHIFT, R, exec, reboot
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Brightness
bind=,XF86MonBrightnessUp,exec,brightnessctl set 5%+
bind=,XF86MonBrightnessDown,exec,brightnessctl set 5%- --min-value=5

# Temperature
# bind= $mainMod, up, exec, busctl --user -- call rs.wl-gammarelay / rs.wl.gammarelay UpdateTemperature n +1000
# bind= $mainMod, down, exec, busctl --user -- call rs.wl-gammarelay / rs.wl.gammarelay UpdateTemperature n -1000

# Speaker volume
bind=,XF86AudioRaiseVolume,exec,pamixer -i 5
bind=,XF86AudioLowerVolume,exec,pamixer -d 5
bind=,XF86AudioMute,exec,pamixer -t
# Mic volume
bind=SUPER,XF86AudioRaiseVolume,exec,pamixer --default-source -i 5
bind=SUPER,XF86AudioLowerVolume,exec,pamixer --default-source -d 5
bind=,XF86AudioMicMute,exec,pamixer --default-source --toggle-mute

# Screenshot
bind=,XF86Calculator,exec,grim -g "$(slurp)"
#bind=,XF86Calculator,exec,XDG_CURRENT_DESKTOP=sway flameshot gui

# Waybar
bind=$mainMod, W, exec, pkill waybar || waybar

# Custom
bind= $mainMod, B, exec, brave
bind= $mainMod, F, fullscreen
bind= $mainMod, C, exec, code
bind= $mainMod, N, exec, nemo
bind= $mainMod, V, exec, cliphist list | rofi -dmenu -theme ~/.config/rofi/launchers/type-2/style-1.rasi | cliphist decode | wl-copy
bind= $mainMod, R, exec, ~/Documents/Dotfiles/Script/toogle_temperature.sh
bind= $mainMod, O, exec, obsidian

bind = $mainMod ALT, L, movecurrentworkspacetomonitor, l
bind = $mainMod ALT, R, movecurrentworkspacetomonitor, r


# Hycov bindings
# bind = $mainMod,tab,hycov:toggleoverview

plugin {
    #hycov {
    #   overview_gappo = 60 # gaps width from screen edge
    #    overview_gappi = 24 # gaps width from clients
     #   enable_hotarea = 1 # enable mouse cursor hotarea     
      #  hotarea_monitor = none # monitor name which hotarea is in, default is all
       # hotarea_pos = 1 # position of hotarea (1: bottom left, 2: bottom right, 3: top left, 4: top right)
        #hotarea_size = 10 # hotarea size, 10x10
        #swipe_fingers = 4 # finger number of gesture,move any directory
        #move_focus_distance = 100 # distance for movefocus,only can use 3 finger to move 
        #enable_gesture = 1 # enable gesture
        #disable_workspace_change = 0 # disable workspace change when in overview mode
        #disable_spawn = 0 # disable bind exec when in overview mode
        #auto_exit = 1 # enable auto exit when no client in overview
        #auto_fullscreen = 0 # auto make active window maximize after exit overview
        #only_active_workspace = 0 # only overview the active workspace
        #only_active_monitor = 0 # only overview the active monitor
        #enable_alt_release_exit = 0 # alt swith mode arg,see readme for detail
        #alt_replace_key = Alt_L # alt swith mode arg,see readme for detail
        #alt_toggle_auto_next = 0 # auto focus next window when toggle overview in alt swith mode
        #click_in_cursor = 1 # when click to jump,the target windwo is find by cursor, not the current foucus window.
        #hight_of_titlebar = 0 # height deviation of title bar hight

    #}
}
